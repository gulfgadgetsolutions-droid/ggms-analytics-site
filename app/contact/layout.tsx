import type { Metadata } from "next";
export const metadata: Metadata = { title: "Contact", description: "Contact GGMS Analytics to discuss a data engineering, analytics, data science, AI automation, or managed services requirement.", alternates: { canonical: "/contact" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
